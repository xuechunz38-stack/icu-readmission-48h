# Data README
Raw data are not committed. This project uses the **eICU demo** dataset from PhysioNet.
After downloading, set an environment variable `DATA_DIR` pointing to the local path, or edit a small `config.yaml`.
